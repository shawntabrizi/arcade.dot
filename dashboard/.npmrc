# First-party @parity/* libraries managed by our team; we want the latest
# versions through release. Overrides the user-level min-release-age guard for
# THIS package only (machine-wide setting in ~/.npmrc is unchanged).
min-release-age=0
